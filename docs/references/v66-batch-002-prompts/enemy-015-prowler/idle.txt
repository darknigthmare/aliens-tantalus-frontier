Use case: precise-object-edit. Input image 1 is the EDIT TARGET: an existing eight-frame Prowler idle sprite sheet. Change ONLY the painted gray-white checkerboard background to perfectly solid pure magenta #FF00FF, including the empty gaps between limbs and inside curled tails. Preserve every existing creature pixel design, all eight poses, exact scale and positions, strict right-facing view, crisp pixel-art finish, colors, anatomy and entire tail. No scene, shadow, gradient, glow, grid lines, text, borders. Output the same exact wide 2:1 canvas composition with four columns and two rows. Background must be flat solid saturated #FF00FF, not transparent, not checkerboard. Do not redesign or reanimate the subject.
