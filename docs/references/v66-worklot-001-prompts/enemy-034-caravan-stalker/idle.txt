Use case: precise-object-edit
Asset type: production 2D game animation sprite sheet
Input image: the corrected all-quadrupedal Caravan Stalker idle sheet.

Primary request: change ONLY uniform scale, centering and background edge cleanup. Preserve the exact eight all-quadrupedal poses, pose order, identity, anatomy, colors, strict right-facing side view and idle continuity.

Correction: uniformly reduce every complete creature to 88 percent of its current size, preserving aspect ratio, then center each creature inside its own square cell at one consistent physical scale. Keep the same relative ground level. Every tail tip, skull, claw and limb must stay at least 24 pixels inside all four edges of its own cell. No creature pixel may touch or cross any cell boundary.

Layout invariant: exact 2:1 wide landscape canvas, exactly 4 columns by 2 rows, strict row-major order, exactly eight poses.
Background invariant: perfectly flat uniform pure #FF00FF magenta across every background and outer-edge pixel.

Constraints: no redesign; no pose change; no upright or bipedal frame; no added or removed anatomy; no duplication; no grid lines; no text; no labels; no shadow; no scenery; no logo; no watermark.
