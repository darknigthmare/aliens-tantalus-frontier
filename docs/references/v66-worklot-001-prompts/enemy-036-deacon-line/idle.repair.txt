Edit only the supplied sprite-sheet canvas and background. Preserve the eight Deacon character renders, anatomy, proportions, poses, lighting, scale, strict right-facing direction, and row-major order unchanged. Recompose them into an exact 2:1 landscape canvas with a precise 4 columns by 2 rows grid of eight equal square cells, one complete character centered inside each cell with generous equal gutters and no touching or overlap. Replace every background pixel outside the characters with one perfectly uniform flat pure #FF00FF RGB magenta, with no gradient, texture, noise, shadow, glow, scenery, grid lines, labels, logo, UI, or watermark. Do not add, remove, redraw, recolor, mirror, crop, or alter any body part.
